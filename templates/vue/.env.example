# Copy this file to .env.local and fill in the values.
# Never commit .env.local to version control.

# Base URL for the backend API. Leave empty to proxy through Vite (/api).
VITE_API_BASE_URL=http://localhost:3000
